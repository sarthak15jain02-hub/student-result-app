apiVersion: v1
kind: Service
metadata:
  name: student-result-service
spec:
  type: NodePort
  selector:
    app: student-result-app
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
    nodePort: 30007